org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true
kotlin.code.style=official

# Single source of truth for the published version. Consumed by the root build
# script and baked into the Gradle plugin via a generated version constant.
ktorkitVersion=0.1.0-SNAPSHOT
